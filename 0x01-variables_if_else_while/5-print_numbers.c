#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10; a++)
		printf("%d", n);
	putchar('\n');
	return (0);
}
